<nav class="navbar navbar-expand-lg fixed-op navbar-dark bg-dark mb-5" aria-label="Main navigation">
  <div class="container-fluid">
    <%= link_to 'EspressoApp', dashboard_index_path, class: 'navbar-brand' %>

    <% if user_signed_in?  %>
      <div class="navbar-collapse offcanvas-collapse flex-grow-0">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'Home', dashboard_index_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Senha', edit_user_registration_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'MFA', settings_mfa_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <% if user_signed_in? %>
              <%= link_to 'Sair',
                    destroy_user_session_path,
                    data: { "turbo-method": :delete, turbo_confirm: 'Deseja realmente sair?' },
                    class: 'nav-link'
              %>
            <% end %>
          </li>
        </ul>
      </div>
    <% end %>
  </div>
</nav>
